import { NameOnlyDirective } from './name-only.directive';

describe('NameOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NameOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
