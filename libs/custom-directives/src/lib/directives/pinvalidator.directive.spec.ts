import { PinvalidatorDirective } from './pinvalidator.directive'

describe('PinvalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PinvalidatorDirective()
    expect(directive).toBeTruthy()
  })
})
