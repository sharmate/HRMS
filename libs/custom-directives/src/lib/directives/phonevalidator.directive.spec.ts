import { PhoneDirective } from './phonevalidator.directive'

describe('PhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneDirective()
    expect(directive).toBeTruthy()
  })
})
